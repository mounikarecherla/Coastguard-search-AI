# Coastguard Search and Rescue Assistance (ML-Powered Platform)

A machine learning-based web platform that assists coastguard teams in locating missing ships using historical and real-time data.

## 🌊 Features

- Predicts likely ship locations using ML
- Visualizes search zones based on past coordinates
- Displays rescue data and logs using CSV files
- Responsive front-end with HTML and CSS
- Built using Python (Flask) for backend logic

## 🧩 File Structure

- `app.py` – Contains ML logic and web server code
- `index.html` – Home/landing page
- `dashboard.html` – Map and results interface
- `style.css` – Styling for UI
- `data.csv` – Ship location or rescue data

## 🚀 How to Run

1. Install Python
2. Install required packages:
   ```bash
   pip install flask pandas matplotlib
